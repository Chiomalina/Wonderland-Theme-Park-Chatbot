{
  "name": "swift-mind-review-summerizer",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "heroku-postbuild": "cd server && npm install && npm run build",
    "start": "node dist/index.js",
    "build": "tsc",
    "dev": "bun run index.ts",
    "format": "prettier --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspace": [
    "packages/*"
  ],
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
